
<table class="table table-bordered">
  <tbody>
    <% @lists.each do |list| %>
      <%= render list %>

      <% if list.completed  %>

        <tr class="table-secondary" >
          <td class="striker" > <%= link_to list.description, edit_list_path(list) %> </td>
          <td>
            <center>
              <%= form_for(list) do |f|  %>
                <%= f.text_field :description, type: 'hidden' %>
                <%= f.check_box :completed, :checked => false, :style => 'visibility: hidden' %>
                <%= f.submit "Uncross", class: 'btn btn-outline-secondary btn-sm' %>
              <% end %>
            </center>
          </td>
          <td> <%= link_to "Delete", list, class: 'btn btn-outline-danger btn-sm ', method: :delete, data: { confirm: 'Do you want to delete these item' } %> </td>
        </tr>

      <% else %>
         <tr class="" >
          <td> <%= link_to list.description, edit_list_path(list) %> </td>
          <td>
             <center>
              <%= form_for(list) do |f|  %>
                <%= f.text_field :description, type: 'hidden' %>
                <%= f.check_box :completed, :checked => true, :style => 'visibility: hidden' %>
                <%= f.submit "Cross Off", class: 'btn btn-outline-secondary btn-sm' %>
              <% end %>
            </center>
          </td>
          <td> <%= link_to "Delete", list, class: 'btn btn-outline-danger btn-sm ', method: :delete, data: { confirm: 'Do you want to delete these item' } %> </td>
        </tr>
      <% end %>

    <% end %>

  </tbody>
</table>

<%= link_to "Delete Marked Items", home_trash_em_path, data: { confirm: 'Do you want to delete these item' } %> |
<%= link_to "Delete All Items", home_trash_em_all_path, data: { confirm: 'Do you want to delete these item' } %>
